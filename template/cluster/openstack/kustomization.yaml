---
namespace: kcm-system
buildMetadata:
  - managedByLabel
  - originAnnotations
generatorOptions:
  disableNameSuffixHash: true
  labels:
    k0rdent.mirantis.com/component: kcm
  annotations:
    projectsveltos.io/template: "true"
secretGenerator:
  - name: openstack-cluster-secret
resources:
  - credential.yaml
  - openstack-standalone-cp.yaml
