---
buildMetadata:
  - managedByLabel
  - originAnnotations
generatorOptions:
  disableNameSuffixHash: true
resources:
  - dev-docker.yaml
  - dev-remote.yaml
  # - dev-openstack.yaml
  # - dev-vsphere.yaml
