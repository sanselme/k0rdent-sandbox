---
buildMetadata:
  - managedByLabel
  - originAnnotations
generatorOptions:
  disableNameSuffixHash: true
resources: []
# - dev-aws.yaml
# - dev-docker.yaml
# - dev-openstack.yaml
# - dev-remote.yaml
# - dev-vsphere.yaml
